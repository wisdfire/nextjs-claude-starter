---
name: prd-authoring
description: PRD(제품 요구사항 문서) 작성 표준. 요구사항 정의·PRD 작성·기획 문서화 시 반드시 사용. 애드센스 심사 80%+ 체크리스트, 유지보수 최소화(cron API + Playwright 크롤링) 설계, Lighthouse 등 객관 완성도 지표를 요구사항으로 기입하는 규칙을 강제한다. 이 하네스의 핵심 차별점.
---

# PRD 작성 표준

## 왜 이렇게 쓰는가 (Why-first)

PRD는 "무엇을 왜 만드는가"를 근거와 함께 확정해 개발팀의 판단 비용을 없애는 문서다. 이 하네스의 PRD는 세 가지를 **반드시 요구사항으로 명문화**한다는 점에서 일반 PRD와 다르다: (1) 애드센스 심사 통과 확률 80%+, (2) 유지보수 비용 최소화, (3) 객관적 완성도 지표. 이 세 가지가 빠지면 나중에 수익화 실패·운영 과부하·품질 미달로 되돌아온다.

## 표준 구조

1. **개요·문제 정의** — 검증된 페인포인트에 근거한 문제.
2. **타깃 사용자** — 수요 신호에 근거한 사용자군.
3. **목표·성공 지표** — 정량 목표(측정 가능).
4. **기능 요구사항** — 우선순위(MoSCoW: Must/Should/Could/Won't)와 함께.
5. **비기능 요구사항** — 성능·보안·접근성·SEO.
6. **애드센스 준비 요구사항** — 아래 (a).
7. **데이터 확보·유지보수 최소화 전략** — 아래 (b).
8. **완성도 검증 지표** — 아래 (c).
9. **품질·테스트 전략** — 아래 (d).
10. **범위 밖·리스크** — 안 하는 것, 미검증 가설.
11. **근거 출처** — 각 요구사항의 소급 근거.

## (a) 애드센스 심사 80%+ 체크리스트 (요구사항으로 기입)

`adsense-readiness` 스킬을 근거로 다음을 PRD에 명문화한다.

- [ ] **오리지널·유용한 콘텐츠** — 자동 수집 데이터라도 편집 부가가치를 더해 복제 아님.
- [ ] **충분한 분량·페이지 수** — 핵심 콘텐츠 페이지 최소치 명시.
- [ ] **필수 정책 페이지 4종** — 개인정보처리방침·이용약관·문의·소개.
- [ ] **명확한 네비게이션·사이트 구조**.
- [ ] **정책 위반 회피** — 유해·저작권 침해·저품질 자동생성 콘텐츠 금지, 광고 배치 정책 준수.

## (b) 유지보수 최소화 원칙 (요구사항으로 기입)

수동 운영을 설계에서 배제한다.

- **cron 기반 자동 API 수집**: 수집 주기·대상·인증·정규화·중복 제거를 명시.
- **Playwright 크롤링 수집**: API가 없는 소스는 크롤러로 자동 확보. 대상·주기·차단 대응 명시.
- **실패 복원력**: 재시도·백오프·실패 알림을 요구사항에 포함. 운영자 개입 지점을 최소화하고, 자동화 불가 항목은 명시적으로 표시.
- **IaC(인프라를 코드로)**: 서버·DB·스케줄러(cron)·캐시 등 인프라를 수동으로 만들지 않고 **OpenTofu 등 IaC로 프로비저닝**하도록 요구사항에 기입한다. 재현 가능성(동일 환경 재생성)·drift 방지(수동 변경 차단)·운영 자동화를 통해 유지보수 비용을 더 낮춘다. IaC 상세 규약(remote state·lock, plan/apply, outputs 연결)은 `opentofu-infra` 스킬을 참조한다.

## (c) 객관적 완성도 지표 (요구사항으로 기입)

주관적 "완성"이 아니라 측정 가능한 기준으로 완료를 정의한다.

- **Lighthouse 4개 카테고리 목표 점수**: Performance / Accessibility / Best Practices / SEO — 각 **90+**(프로젝트 기준에 맞게 조정 가능하되 수치 명시).
- **Core Web Vitals 임계값**, **접근성 위반 0건**, **단위 테스트 통과·커버리지** 등을 완료 기준(acceptance)으로 연결.
- **테스트 통과율을 완성도 지표에 포함**: Lighthouse 등 객관 지표와 짝으로, 아래 (d)의 **테스트 통과율(단위/통합/E2E)과 커버리지 목표치**를 완료 기준으로 명시한다.

## (d) 품질·테스트 전략 (요구사항으로 기입)

"동작한다"를 주관이 아니라 통과하는 테스트로 증명하도록, 요구사항 단계에서 테스트 범위를 확정한다.

- **테스트 범위 정의**: **단위(unit)·통합(integration)·E2E** 각 계층에서 무엇을 검증할지 명시한다(핵심 비즈니스 로직·API 계약·주요 사용자 흐름).
- **커버리지 기대치**: 핵심 모듈의 최소 커버리지 목표(예: 라인/브랜치 %)를 수치로 기입한다. 프로젝트 기준에 맞게 조정하되 반드시 수치를 남긴다.
- **CI 게이트**: **테스트가 통과해야만 배포**되도록 CI 게이트를 요구사항으로 명문화한다(테스트 실패 시 배포 차단).
- **완성도 연결**: 위 (c)의 완성도 지표에 테스트 통과율을 포함해, Lighthouse 점수와 테스트 통과가 함께 충족돼야 완료로 판정한다.

## 작성 규칙

- **근거 추적성**: 모든 요구사항은 `_workspace/03_validated_findings.md`의 검증된 발견으로 소급 가능해야 한다. 근거 없는 기능은 넣지 않는다.
- **환각 금지**: 미검증 시장 수치·주장을 단정으로 쓰지 않는다. 가설은 "가설"로 표기.
- **스택 정합**: Next.js 16 · Supabase · Drizzle · Playwright · cron 전제. 벗어나는 요구는 표시해 tech-verifier 검증 대상으로 남긴다.
- **완료 판정**: (a)(b)(c)(d)가 모두 요구사항으로 반영되고 각기 검증 가능한 완료 기준이 붙어야 PRD를 완료로 본다. (b)에는 IaC 원칙이, (d)에는 테스트 전략·CI 게이트가 포함돼야 한다.

## 산출물

- 최종: `docs/PRD.md`. 재작성이 아니라, 재호출 시 기존 문서를 읽고 변경분만 최소 편집한다.
